import { Text, View } from 'react-native';

export default function PostsScreen() {
	return (
		<View style={authStyles.container}>
			<Text style={authStyles.title}>Posts Screen</Text>
		</View>
	);
}
